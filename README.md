# Adding ChildView (Swift)

This is simple project sample to demonstrate adding multiple child views.

This way of adding child view are very useful while making the custom tab bar like UI or switching multiple views on segment control or buttons.
